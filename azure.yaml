# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: githubiq
metadata:
  template: githubiq@0.1.0
services:
  backend:
    project: ./backend
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
  worker:
    project: ./backend
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
  frontend:
    project: ./frontend
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
